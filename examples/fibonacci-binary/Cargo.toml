[package]
name = "fibonacci-binary"
version = "0.1.0"
edition = "2021"

[dependencies]
jolt-sdk = { path = "../../jolt-sdk", features = ["std"] }
guest = { package = "fibonacci-binary-guest", path = "./guest" }
